version: '3.8'

services:
  taskmanager:
    build: .
    container_name: taskmanager
    ports:
      - "8080:8080"
    volumes:
      - ./tasks.db:/app/tasks.db
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
